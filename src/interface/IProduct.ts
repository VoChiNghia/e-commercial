export default interface IProduct{
    categorySlug: string
    colors: string[]
    description: string
    image01: string
    image02: string
    price: string
    size: string[]
    slug: string
    title: string
}

